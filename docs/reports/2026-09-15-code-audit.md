# 2026-09-15 当前代码审查报告

本轮完成模型客户端、章节任务、资料与存储、规划与导出、Web 操作链路和依赖的检查，修复了可复现问题。最终 47 个测试文件、948 项测试通过，8 个实际浏览器场景通过，依赖审计未报告已知漏洞。这些结果不等于所有模型和连续创作质量均已验收。

审查基线为 `c5b3359`，包含当时尚未提交的指定章节规划修复（45 文件 / 920 项测试）。实现和复核均由当前代理完成，没有使用独立 Agent。本轮自动化与浏览器验证使用本地脚本模型、测试凭证和临时作品，真实模型调用为 0；用户作品没有作为修复对象。

## 发现与修复

下表的优先级针对修复前影响：P1 为数据、凭证或采用边界问题，P2 为操作与展示错误。所列代码问题均已修复；真实内容质量的后续事项另列。

| 优先级 | 触发与影响 | 修复与主要证据 |
| --- | --- | --- |
| P1 | Chat／Claude 流中断，或 Chat 报 aborted／资源不足时，已经生成的文字随错误丢失；续写中断也丢失新增部分 | 客户端返回可见 `partialText`，初写保存片段，续写保存原前缀与新增文字；保持 C4 未完成，不运行 C5。`chat-client`、`claude-client`、`chat-chapter`、`draft-rewrite` 回归验证重开和再次续写 |
| P1 | 工具轮数耗尽或响应仍为 pause_turn／无停止原因时，C4 可能将已有文本送入检查并成为 ready 稿 | 只将 end_turn／stop_sequence 视为完整结束；其余保存为未完成片段。Chat 声称 tool_calls 却没有工具列表时明确失败。`chapter-task`、`chat-client` 回归 |
| P1 | Claude SDK 跟随 307 后把 x-api-key 发往另一端点；代理错误可能回显凭证到任务和页面 | 禁止跟随重定向，错误遮蔽配置 key 与 Bearer token，限制错误长度。本地两个 HTTP 端点实测：修复前目标收到测试 key，修复后未收到请求。`claude-client` 回归 |
| P1 | 仅绑定 127.0.0.1 仍允许外部网页通过 text/plain 简单 POST 创建作品 | 校验回环 Host、同源 Origin，API POST 必须为 JSON；非法 JSON／路径、超限、错误媒体类型分别返回 400／413／415。`http-boundary` 测试与浏览器跨源 POST 验证 |
| P1 | Windows 中大小写不同的作品地址映射同一目录，却创建两个独立 Session，页面读到旧状态并可能覆盖另一页修改 | 以 `realpathSync.native` 的真实目录作为会话键，共享状态和活动任务；普通 realpathSync 未统一大小写，实测后改用 native。`workspace` 验证两种地址交替保存仍保留全部改动 |
| P2 | 主 Agent 只有“下一章”工具，作者要求两章分别安排时，只保存下一章却可能回复全部完成 | `plan_add_to_chapter` 支持明确 targetChapter；旧名称兼容，非法章号不回退。多章逐项保存并核对进度。前序新增 8 项 `story-progress` 回归；真实 Gemini 复测仍待进行 |
| P2 | 写章、手动保存、确认并写的响应迟到，会把已经离开的用户带回旧稿；切换方案或章节可能显示旧选择／正文 | 操作结果仅更新仍在原 URL 的页面；已提交操作继续完成。方案和阅读页按路由重建。浏览器复现并验证迟到响应、深链接、切章和复合动作 |
| P2 | 手机阅读页保留两列布局，正文被挤成很窄一列；main 内部溢出被顶层宽度检查漏掉 | 手机改为单列并调整正文间距，取消侧栏固定；390px／320px 检查 main 自身溢出并查看截图 |

Vite 开发代理另有配套修复：字符串形式的 proxy 会改写 Host，开启来源校验后，真实开发页面保存返回 403。现改为对象形式并显式 `changeOrigin: false`；用实际 Vite 和 Chromium 验证保存成功，没有放宽后端来源检查。

不完整的局部改写 JSON 仍不会应用替换；拒绝响应、思考内容和残缺工具参数不作为可用正文或可执行工具。已完成正文的 C5 失败仍只恢复必要检查。

## 依赖

| 依赖 | 原锁定版本 | 新锁定版本 |
| --- | --- | --- |
| Vite | 6.3.5 | 6.4.3 |
| Vitest | 3.2.4 | 4.1.11 |
| YAML | 2.8.1 | 2.8.3 |

初始审计有 4 个受影响包，含 critical／high 项。Vitest 3.2.7 的依赖链仍保留 mocker 路径穿越问题，因此选用兼容 Vite 6 和当前 Node 的 4.1.11。同步更新 package.json 和锁文件，未使用 `npm audit fix --force`；最终 `npm audit --json` 的 total 为 0。该结论以本次审计数据库和锁文件为准。

## 验证记录

环境：Windows、Node v24.9.0、Vitest 4.1.11、Vite 6.4.3。

| 检查 | 结果 |
| --- | --- |
| `npm test` | 47 文件 / 948 项通过；本轮审查新增 28 项，之前指定章节修复新增 8 项 |
| `npm run typecheck` | 服务端与 Web 类型检查通过 |
| `npm run web:build` | 构建通过 |
| `npm audit --json` | 0 个已知漏洞 |
| `git diff --check` | 无空白错误 |
| Chromium 浏览器 | 下列 8 个场景通过，未捕获页面脚本错误；2 次本地模型替身调用，0 次真实调用 |

| 浏览器场景 | 核对内容 |
| --- | --- |
| draft-late-start | 写章启动响应迟到不会跳回旧页面 |
| draft-late-manual-save | 手动修改保存后不会劫持已离开的页面 |
| preparation-query-selection | 方案深链接切换后展示正确方案 |
| reader-chapter-switch | 新章加载时不在新标题下显示旧正文 |
| preparation-late-write | 确认并写继续执行，迟到响应不强制导航 |
| browser-cross-origin-post | 浏览器外部 Origin 简单 POST 被 403 拒绝 |
| vite-browser-write | 实际 Vite 页面可以正常保存资料 |
| mobile-reader | 390px／320px 阅读布局、main 内部宽度及截图核对 |

本机浏览器证据：`C:/Users/Administrator/AppData/Local/Temp/nf-code-audit-browser-naL4k7/`，含 `verification.json`、`browser.source.mjs`、8 张场景截图及两种手机宽度截图。脚本备份 SHA256 为 `BC59BAA232D8540F1A26D584E1EE952AFDDA9841B2C40C7DCC2276A7AE2E263C`，与工作树临时脚本一致。临时浏览器、测试 HTTP 服务及 Vite 已关闭；测试作品和工装不提交到仓库。

审查也检查了任务输入冻结、同请求去重、暂停与恢复、至多一次自动修订、采用事务失败恢复、候选隔离、规划生命周期、固定版本导出以及对话队列；既有相关业务回归全部通过。没有把测试项数当作业务设计或文本质量的完成证明。

## 主目录同步与运行

代码提交 `cd17226`（指定章节规划）、`81f533e`（运行与 Web 修复）、`5381a9e`（依赖升级）已快进合入主目录 master 并推送，`git ls-remote` 已核对远端为 `5381a9eeceaf69e95246b411542b4d0d5f2c8ebc`。本报告及记忆由随后的文档提交保存。

主目录首次 `npm ci` 遇到 Windows EPERM：旧 Web 服务的 esbuild 子进程占用可执行文件。核对旧服务使用 data/demo、下一章无草稿且没有活动网络连接后，停止该服务及编译子进程，再次安装成功；未修改锁文件来绕过问题。主目录随后再次通过 948 项测试、类型检查与构建，安装审计仍为 0 个已知漏洞。

以原 data/demo 和 5174 端口重启新后端，读取首页、作品列表、overview 和 tasks 均正常，跨 Origin 请求返回 403。首页加载本次构建，demo 正式进度仍为第 52 章，活动任务为 0；主服务保留运行，独立真实验收服务没有启动。255 个作品和配置文件的同步前后 SHA256 完全一致，无新增或删除。启动日志及主目录核验记录保留在上述证据目录。

## 仍需后续验证的范围

- 同一《雾港封签》三章真实验收尚未完成：第一章 ch1d3 已采用；第二章 ch2d1 虽为 ready，正文提前排除了第三章需匹配的旧印钳，尚未采用，须修订；第三章未生成。需要先通过对话补存 F02 在第三章 full 的安排，并核对真实工具结果。
- 指定章节规划修复没有重新调用 Gemini 验证；DeepSeek 官方没有对应凭证，当前只有本地协议与业务工作流覆盖。Claude 的本轮测试也全部是本地模拟。
- 连续创作中的人物动机、视角和伏笔语义不能只靠结构检查证明；剩余用户流程 §10 审计继续随真实验收进行。
- 保存与并发协调仍限单服务进程；没有跨进程写锁，不保证强制结束进程前尚未写盘的流片段。主 Agent 完整历史仍无自动压缩。

本轮交付限代码审查与修复、文档和 Git 同步；已暂停的“继续完成项目”总目标保持暂停。
