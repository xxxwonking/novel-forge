# 对话规划的明确目标章节

真实验收中，作者要求第 2 章部分兑现、第 3 章完整兑现。旧主 Agent 只提供 `plan_add_to_next_chapter`，实际只保存第 2 章，却在回复中声称两章都已安排。证据是同一验收作品的 `12-say-result.json` 及调用 27；第三章节拍的 `resolves` 仍为空。

修复沿用已确认的后续规划范围与共享 PlanningService：向主 Agent 提供 `plan_add_to_chapter`，可指定 `targetChapter`，省略时为下一章；历史工具名继续可执行。明确章号无效时拒绝，不回退到下一章。多章安排分别保存并返回实际目标章节，最终回复须据工具结果核对。

- [x] 业务回归先复现指定未来章节没有保存、错误章号不能悄悄改下一章。
- [x] 新工具及历史入口共用参数校验和 PlanningService；保留计划与正式事实隔离。
- [x] 相关测试、类型检查、完整检查：新增 8 项，45 文件 / 920 项通过，typecheck、web:build 通过。
- [ ] 同一真实作品补存第三章安排并核对。

当前代理独立实现与复核，不使用子代理；继续真实验收时保留第二章原稿，质量修订另建版本。
