# 真实验收发现的 C5 引用与人物状态校验

依据：持续目标中的问题检查，以及用户流程 §6.2、§10 的正文与结构一致性。当前代理独立实施与复核，沿用已授权范围。

Gemini 首章真实响应把人物姓名填入编号字段；事件参与者和人物出场被静默过滤，人物状态仅产生解析警告但未阻止声明成功。进一步核对发现 `location` 投影只支持旧式数字编号，无法保留资料服务合法生成的 `S_OldArchive`。原始正文和响应保存在独立验收作品中，未采用。

- [x] 先用真实章节任务复现非法人物/情节线引用及未支持的状态字段被接受，断言失败应保留正文、阻止采用，恢复只重做 C5。
- [x] 复现合法非数字地点 ID 被投影为 null；修复其与资料 ID 规则的差异。
- [x] 在解析边界拒绝未知引用、未支持的状态字段和非法生存/地点值；不猜测同名人物。旧正文与历史记录保持可读。
- [x] C5 明确给出允许的 ID 和状态字段，要求逐字复制原文；任务与遗留 pipeline 共用提示与拒绝逻辑。
- [x] 跑相关与完整测试、类型检查和构建，保存根因与验证记录。
- [ ] 在同一验收作品继续真实三章，不重复首章生成或已有对话。

验证结果：新增 12 项回归，完整 45 文件 / 912 项通过，typecheck、web:build 通过。纠错另复现并统一了 `vital: unknown` 的接受范围。按用户最新要求先保存并提交；服务已恢复读取同一作品后停止，没有增加真实调用。继续位置见 MEMORY 第 17 节末尾。
