# 后台任务受理后的对话交接

真实验收调用 36–46 复现：修订任务已保存，主 Agent 却重复查询稿件，耗尽 8 轮后回复“请把要做的事说得更具体一点”。第二章 ch2d2 已完成检查并被字数闸门拦截，对话提示没有反映实际动作。

沿用已确认的后台任务与结果页设计。只补提示容易继续轮询；等待整章完成会延长对话并阻塞后续指令。选择在已提交任务的完整工具轮结束后交接：保存原始 assistant 与全部工具结果，返回由真实 effect 生成的任务入口说明，不继续调用模型轮询。正式采用仍由既有业务入口控制。作者同轮要求的资料与计划应先保存，再提交后台任务。

- [x] 先用失败回归复现修订/写章/检查后的重复模型调用及不准确回复。
- [x] 保留采用并继续、暂停/结束请求、失败动作和同步纠错后继续检查；不截断同轮工具历史。
- [x] 验证 Chat 思考与工具签名跨会话完整回放，不伪造最终模型消息。
- [x] 运行相关测试、完整检查，再在同一真实作品验证修订与第三章启动。

结果：新增 6 项回归，更新既有集成夹具并保留采用、暂停与完整历史断言。真实 Gemini 的 `16-say`、`23-say`、`25-say` 分别经过 2、3、3 次主 Agent 调用后交接；受理之后没有额外主 Agent 轮询。第三章生成中实际离开页面，返回仍是同一任务。完整验证连同作者要求传递修复共 48 文件 / 962 项通过，类型检查与 Web 构建通过。

当前代理独立实现和复核。用户已授权继续现有验收及发现问题后的修复，不引入新的自动采用或多章生成范围。
